# 🥾 d2 — бот для мини-игры «Сапожный снос» (Boot Breaker)

> Компьютерное зрение + автоматизация ввода для Arkanoid-мини-игры Dota 2: бот «видит» сапог
> и тележку, предсказывает траекторию и ведёт платформу под точку падения.

## Оглавление
- [О проекте](#о-проекте)
- [Как это работает](#как-это-работает)
- [Установка](#установка)
- [Использование](#использование)
- [Конфигурация](#конфигурация)
- [Структура](#структура)
- [Тесты](#тесты)
- [Ограничения и ToS](#ограничения-и-tos)

## О проекте

«Сапожный снос» — Breakout-клон из ивента Dota 2 (Crownfall / Dark Carnival): сапог
отскакивает от тележки-платформы, разбивая блоки. Задача бота — ловить сапог: захват
экрана → детекция сапога/тележки → предсказание точки падения с учётом отскоков от стенок →
движение платформы.

**Цель окружения:** Windows, игра в режиме borderless 1920×1080.

## Как это работает

```
захват (dxcam) → детекция (OpenCV) → трекинг+предсказание → решение → ввод (pydirectinput)
```

Точка падения зависит только от **наклона** скорости сапога (vx/vy), поэтому скорость берётся
как разница позиций между кадрами — без привязки к реальному времени. Отскоки от левой/правой
стенок моделируются «треугольной волной» (`src/track.py: reflect_1d`). Подробности —
[LOGIC.md](LOGIC.md) и [ARCHITECTURE.md](ARCHITECTURE.md).

## Установка

```bash
python -m pip install -r requirements.txt
```

## Использование

> ⚠️ **Игра встаёт на паузу без фокуса окна.** Для боя это не мешает: бот захватывает экран из
> фонового процесса, а ввод уходит в активное окно — держи игру в фокусе, терминал фокус не
> забирает. Для ручного снятия живого кадра используй `--delay` (успеть кликнуть по игре).

**1. Снять опорные кадры** (для настройки детекции оффлайн):
```bash
python -m src.capture --save assets/frames/inflight.png --delay 3
```
Захват — весь рабочий стол 1920×1080; мини-игра детектируется как панель по центру.

**2. Проверить детекцию на кадре** (печатает координаты + сохраняет разметку):
```bash
python debug/overlay_frame.py assets/frames/inflight.png
```

**3. Запустить бота.** Управление — **A/D**, пуск/подтверждение — **пробел**. Бот сам
запускает сапог, ловит его и **проходит переходы между уровнями** (единая модель состояний:
летит → ловим; висит в пред-пуске / экран перехода → жмём пробел).

```bash
python -m src.main
```
- По умолчанию: пауза 2 с (успеть сделать игру активной) → автозапуск сапога → ловля.
- `--manual` — не запускать сапог автоматически (пуск руками, бот только ловит).
- `--test-launch` — прогнать **только** секвенцию пуска и выйти (подбор пауз/удержания вживую).
- `--delay N` — пауза перед стартом, с.

> Тайминги пуска (`LAUNCH_CONFIRM_PAUSE_S`, `LAUNCH_AFTER_PAUSE_S`, `KEY_TAP_HOLD_S`) —
> в `config.py`; подстрой через `--test-launch`, если сапог не стартует.

> ⚠️ Бот перехватывает клавиатуру и должен держать **окно игры активным**. Аварийный стоп —
> **F12** (нужен пакет `keyboard`) или **Ctrl+C**; при выходе клавиши гарантированно отпускаются.

**Стратегия ловли (v1):** платформа **следует за x сапога**. По данным реального спуска
траектория к тележке почти вертикальна, а предсказание точки падения по шумной скорости даёт
худший результат — поэтому оно выключено (`config.USE_LANDING_PREDICTION`), но оставлено для
будущих угловых бросков.

## Конфигурация

Все пороги и геометрия — в [src/config.py](src/config.py): ROI-соотношения поля, HSV-пороги
тележки/сапога, мёртвая зона управления, клавиши, бюджет цикла. Значения детекции — **первый
прикид**, требуют валидации на реальных кадрах (см. [BACKLOG.md](BACKLOG.md)).

## Структура

```
src/       capture · detect · track · control · input · main · config
tests/     юнит-тесты чистой логики (track, control)
assets/    frames/ — опорные кадры для оффлайн-тестов детекции
debug/     overlay_frame.py — визуальная проверка детекции
```

## Тесты

```bash
python -m pytest -v
```
Покрывают чистую логику (отскоки, предсказание точки падения, трекер, управление) — без игры.
Детекция и управляющий цикл проверяются на реальных кадрах / вживую (§3 CLAUDE.md).

## Ограничения и ToS

⚠️ Любой сторонний софт, автоматизирующий действия в игре, формально попадает под определение
читов Valve/Steam Subscriber Agreement — даже для одиночной ивент-мини-игры. Проект — **учебный
CV/automation**, без обхода анти-чита и вмешательства в процесс игры.
